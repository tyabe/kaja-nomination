<ul class="nav nav-tabs">
  <li> <%= link_to tag_icon(:list, pat(:list)), url(:archives, :index) %></li>
  <li class="active"> <%= link_to tag_icon(:plus, pat(:new)), url(:archives, :new) %></li>
</ul>
<div class="tabs-content">
  <% form_for :archive, url(:archives, :create), :class => 'form-horizontal' do |f| %>
    <%= partial 'archives/form', :locals => { :f => f } %>
 <% end %>
</div>
